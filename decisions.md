# 의사결정 기록

## 데이터 소스
- schedules 대신 pbp를 추가한 이유: 턴오버·패스야드 등 세부 스탯이 schedules엔 없음

## 분석 방법
- 정규시즌만 필터링: 플레이오프는 성격이 달라 섞으면 해석이 흐려짐
- 팀 관점으로 통일(concat): 턴오버는 팀별, 승패는 홈/원정 기준이라 그대로는 결합 불가
- 패스야드 구간화(pd.cut) 이유: 연속값이라 값별 승률이 무의미(값마다 1~2경기) → 구간으로 묶어야 경향이 보임
- 구간 경계를 사분위수(176/221/272)로 잡은 이유: 각 구간에 경기를 고르게(132~141개) 배분해야 비교가 공정
- 평균 대신 구간별 승률을 본 이유: 평균(219 vs 231)은 차이가 작아 보였으나, 구간화하니 42.6%→54.8% 격차가 드러남
- 페널티는 penalty_team으로 묶은 이유: 페널티는 공격/수비 가리지 않고 반칙한 팀이 받으므로, posteam(공격팀)이 아니라 반칙 주체 기준이어야 정확
- 페널티도 pd.cut으로 구간화한 이유: 처음엔 정수값이라 값별 집계했으나, 페널티 12~19개 구간이 표본 1~10경기라 "19개 승률 100%" 같은 노이즈가 나옴. .describe()로 사분위(25%=4, 50%=6, 75%=8) 확인 후 [0,4,6,8,20] 경계로 묶으니 경향(52.9%→45.3%)이 깔끔하게 드러남. 표본 적은 구간은 승률을 신뢰할 수 없다는 교훈
- 3rd down은 비율(성공÷시도)로 집계한 이유: 턴오버·페널티는 횟수 합이지만, 3rd down은 시도 수가 경기마다 달라 단순 성공 횟수로는 비교 불가 → 전환율로 정규화
- 3rd down만 분석한 이유: 미식축구는 4다운 내 10야드 전진으로 공격권을 유지하는데, 3rd down은 사실상 마지막 기회(실패 시 대부분 펀트로 공격권 상실)라 "전환" 개념이 가장 의미 있음. 1st/2nd는 개념도 약하고 nflverse에 컬럼도 없음, 4th는 표본 부족(펀트/필드골)
- 상관계수(corr)로 요인 강도를 비교한 이유: 표의 승률 비교는 눈대중이라, 요인별 승패 상관을 숫자로 뽑아 순위를 객관화
- factors_clean에서만 rate→third_down_rate로 rename: 상관 출력 가독성 위해 (집계 코드의 rate는 유지)

## 성능·구조
- SQLite 캐싱(nfl.db) 도입: pbp 데이터를 매번 다운로드하던 것을 로컬 DB에 저장 후 재사용. os.path.exists로 파일 유무 판단 → 있으면 read_sql, 없으면 다운로드+to_sql
- os.path.exists 체크는 sqlite3.connect보다 먼저 해야 함: connect는 파일이 없으면 그 자리에서 빈(0바이트) 파일을 생성함. 그래서 connect를 먼저 하면 exists가 항상 True가 되어 다운로드 분기(else)로 절대 안 감. 존재 여부를 db_exists 변수에 미리 담아두고 connect는 그 뒤에 하는 순서로 해결
- nfl.db를 .gitignore(*.db)로 제외한 이유: 용량이 크고(185MB) 코드로 재생성 가능하므로 git 관리 불필요 (venv 제외와 같은 논리)

## 시각화
- bar chart를 택한 이유: turnovers_summary·thirddown_summary가 이미 값/구간별로 win_rate를 집계해둔 요약 데이터라, 카테고리 간 비교엔 막대그래프가 적합 
- plt.figure()로 새 figure를 명시한 이유: matplotlib은 이전 plt.bar 상태(현재 figure)를 이어받으므로, 새로 선언하지 않으면 두 그래프가 한 figure에 겹쳐 그려짐
- plt.show() 대신 savefig 사용한 이유: 스크립트 실행(로컬 배치) 환경이라 화면 출력 대신 파일로 저장해 README·문서에 바로 첨부 가능하게 함
- 파일명을 `{feature}_winrate.png` 형식으로 통일한 이유: 이후 패스야드·페널티 등 시각화를 추가해도 네이밍 규칙이 일관되게 유지되도록
- 상관계수 종합 그래프에 axhline(0) 추가: 상관계수는 음수·양수가 섞여 있어 0 기준선을 그어야 "이기는 요인 vs 지는 요인"이 시각적으로 구분됨

## 예측 모델
- [중요·한계] 로지스틱 회귀에 쓴 요인(턴오버·패스야드·페널티·3rd down)은 모두 경기 종료 후 나오는 박스스코어 → 이걸로 그 경기 승패를 맞추는 건 data leakage. "예측"이 아니라 "결과의 사후 설명"에 해당. accuracy가 높아도 예측력의 증거가 아님
- 로지스틱 회귀 도입 이유: 상관계수는 요인을 하나씩 따로 봤으나, 로지스틱 회귀는 4개 요인을 함께 넣어 각 요인의 "순수 기여도"를 봄. 관련성 분석 → 기여도 분석으로 한 단계 확장
- factors_clean을 X(요인 4개)와 y(won)로 분리: ML은 입력(X)과 정답(y)을 나눠 학습시켜야 하므로 drop(columns='won')으로 구성
- train/test 분리(8:2, random_state=42)로 정직하게 평가: 전체 평가(0.69)와 test 평가(0.67)가 거의 같아 오버피팅 거의 없음 확인. 단 train/test 분리는 오버피팅을 거른 것이지 data leakage를 해결한 게 아님 (둘은 별개 문제)
- 계수(coef_) 해석: 3rd down 전환율(+2.27)이 가장 큰 기여, 턴오버(-0.89) 두 번째. 상관계수 순위(턴오버 1등)와 달라진 이유는 상관계수는 요인별 개별 관계, 계수는 여러 요인을 함께 넣은 순수 기여도라서(겹치는 정보가 조정됨). 단 요인별 스케일 차이로 계수 크기 직접비교엔 표준화 필요
- [다음 방향] 진짜 예측은 경기 전 feature(시즌 누적 평균, 홈/원정, 휴식일, 팀 강도)로 구축해야 함. 정확도는 낮아지지만(NFL 예측 천장 65~70%) 그게 정직한 예측 모델. 프로젝트 주제를 "무엇이 승패에 중요한가" → "어떤 스탯이 진짜 예측력 있고 어떤 건 결과 설명일 뿐인가"로 피벗

- [예측 모델 구축] schedules 기반으로 경기 전 feature(home_rest, away_rest, div_game, spread_line)만 써서 홈팀 승패 예측. 박스스코어 안 쓰므로 leakage 없음
- 예측 모델에 2024+2025 둘 다 쓴 이유: schedules 기반이라 pbp(2025)와 독립적이고, 데이터 많을수록 예측 학습에 유리
- home_win을 (home_score > away_score)로 만든 이유: y(정답)는 결과에서 뽑아도 되지만, X(feature)에는 경기 후 스탯을 절대 넣지 않는 게 leakage 방지 핵심
- 기준선(baseline) 설정: 무조건 홈팀 승리로 찍으면 53.5% → 모델은 이걸 넘어야 의미 있음
- [핵심 결과] 예측 모델 test 정확도 67%. 설명 모델(박스스코어)도 67%로 동일 → 경기 후 스탯을 다 알든, 경기 전 정보만 알든 정확도가 같음. 즉 박스스코어의 높은 정확도는 착시이고, 실제 예측력은 경기 전 정보(특히 spread_line에 응축된 시장 예측)에 있음. "설명"과 "예측"은 다른 문제라는 프로젝트 핵심 메시지를 데이터로 입증
- [다음 검증] spread_line을 빼고 나머지(휴식일·디비전)만으로 돌려서, 정확도가 spread_line에 의존하는지 확인 예정